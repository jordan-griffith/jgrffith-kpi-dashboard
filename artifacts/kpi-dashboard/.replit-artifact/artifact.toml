kind = "web"
previewPath = "/"
title = "KPI Dashboard"
version = "1.0.0"
id = "artifacts/kpi-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20631

[services.development]
run = "pnpm --filter @workspace/kpi-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kpi-dashboard", "run", "build" ]
publicDir = "artifacts/kpi-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20631"
BASE_PATH = "/"
